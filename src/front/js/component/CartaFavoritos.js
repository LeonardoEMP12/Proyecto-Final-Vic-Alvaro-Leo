import React from "react";

const CartaFavoritos = ({ title }) => {
  return (
    <div id="CardJuego" className="card mb-3">
      <div className="card-header">{title}</div>
    </div>
  );
};

export default CartaFavoritos;
